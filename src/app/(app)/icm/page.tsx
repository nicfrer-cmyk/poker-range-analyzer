import { ComingSoon } from "@/components/ui/ComingSoon";

export default function IcmPage() {
  return (
    <ComingSoon
      wave="Wave 3 — Depth (Pro)"
      title="ICM Calculator"
      description="See the real money value of your tournament stack — how decisions near the bubble or the final table change once chip value stops being linear. Coming soon."
    />
  );
}
